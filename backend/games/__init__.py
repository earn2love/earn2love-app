"""Earn2Love Play Together — reusable production game platform.

One authoritative engine (FastAPI) powers 50+ games via a small set of reusable
mechanics + per-game definitions/content. Firestore is the server-written
real-time sync layer for mobile clients (clients never write authoritative state).
"""
